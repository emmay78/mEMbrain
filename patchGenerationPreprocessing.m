function [] = patchGenerationPreprocessing(inputImage,labelImage, patchDensity, patchSize)
%PATCHGENERATIONPREPROCESSING preprocesses input and label images before patch generation
%   Images are preprocessed, including resizing, contrast, and coloring,
%   per user specifications. They are then passed to the [TO BE FILLED] for
%   patch generation.

outputArg1 = inputArg1;
outputArg2 = inputArg2;
end


